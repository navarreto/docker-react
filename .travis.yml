language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t navarrete/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true navarrete/docker-react npm run test -- --coverage